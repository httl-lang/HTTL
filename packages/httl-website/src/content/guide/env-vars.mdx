# Environment Variables
 

!!!!! rethink because it may violate security principles

 HTTL_VAR_from_env

Environment variables are a way to store configuration values that can be used in your queries.

They are useful for storing values that are shared across multiple queries, such as base URLs, authentication tokens, or API keys.